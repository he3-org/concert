---
task: "stage-registry"
title: "Create stage-registry.jsonc and user guidance templates"
depends_on: []
wave: 1
model: sonnet
---

## Context

Read these files for full context:
- `docs/concert/missions/2026-03-29-centralize-workflow-stages/ARCHITECTURE.md` — full schema and design
- `docs/concert/workflows/CONCERT-WORKFLOW-MISSION-FULL.md` — current stage table (source of truth for stage data)
- `concert.jsonc` — project config conventions (JSONC format example)

## Task 1: Create stage-registry.jsonc

**Files to create:**
- `docs/concert/stage-registry.jsonc`

**Requirements:**
1. Create the registry file exactly matching the schema defined in ARCHITECTURE.md § Data Model
2. Include all 8 stages: vision, requirements, architecture, ux, tasks, execution, verification, retrospective
3. Include the `workflows` object with all 3 variants (mission-full, mission-medium, mission-small)
4. Use JSONC format with comments explaining the structure
5. Add the managed file header: `// ⚠️ AUTO-GENERATED BY CONCERT — DO NOT EDIT`

**Acceptance criteria:**
- [ ] File is valid JSONC (parseable by `jsonc-parser`)
- [ ] All 8 stages present with all required fields (name, order, agent, inputs, outputs, output_template, triggers_review, produces_spec, interactive, display_name)
- [ ] All agent filenames reference existing files in `.claude/agents/`
- [ ] Workflow variants match the stage lists in the existing workflow markdown files
- [ ] `mission-full` has 8 stages, `mission-medium` has 7 (no ux), `mission-small` has 5 (no requirements, architecture, ux)

## Task 2: Create user guidance templates

**Files to create:**
- `docs/concert/templates/user-guidance.md`

**Requirements:**
1. Create the templates file as defined in ARCHITECTURE.md § D5
2. Include templates for: stage draft complete, stage accepted, review prompt, failure recovery, mission status
3. Use `{variable}` syntax for substitution: `{stage_name}`, `{stage_display}`, `{document_path}`, `{next_stage}`, `{mission_path}`
4. Include both CLI and GitHub UI variants (per `concert.jsonc` → `user_guidance.show_both_cli_and_ui_options`)
5. Add the managed file header

**Acceptance criteria:**
- [ ] All 5 template patterns present
- [ ] Variables are consistent with registry field names
- [ ] Both CLI and UI variants included
- [ ] Managed file header present

## Task 3: Add registry and templates to live-file shipping

**Files to modify:**
- `src/lib/copy.ts`

**Requirements:**
1. Add `docs/concert/stage-registry.jsonc` to live-file shipping — either as a new `LIVE_FILE_SOURCES` entry or as an individual file copy (similar to how rules are handled)
2. Add `docs/concert/templates` as a new `LIVE_FILE_SOURCES` entry (directory copy)
3. Update `countLiveFiles()` to count the registry and templates

**Acceptance criteria:**
- [ ] `copyLiveFiles()` copies `stage-registry.jsonc` to target projects
- [ ] `copyLiveFiles()` copies `templates/` directory to target projects
- [ ] `countLiveFiles()` includes counts for new files
- [ ] Existing tests still pass
